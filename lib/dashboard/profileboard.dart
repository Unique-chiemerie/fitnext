import 'package:flutter/material.dart';

class profileb extends StatelessWidget {
  const profileb({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('something righ?'),
      ),
    );
  }
}
